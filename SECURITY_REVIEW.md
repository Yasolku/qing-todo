# 轻待办安全与质量检查

检查范围：本次公开发布版本

## 已修复

- Electron 从 27.3.11 升级到 43.4.1，electron-builder 从 24.x 升级到 26.15.3。
- `npm audit` 从 15 项已知依赖漏洞降为 0 项。
- 为渲染进程启用 Chromium 沙箱，并保持 `nodeIntegration: false` 与上下文隔离。
- 添加内容安全策略，禁止网络连接、外部脚本、插件对象和表单提交。
- 拦截新窗口和非应用页面导航。
- 使用受限的 `qing-todo://` 自定义协议加载界面，仅允许读取界面与素材目录，并关闭 `file://` 的额外权限。
- 所有 IPC 调用都会核对发送页面，只接受应用自身渲染进程。
- 构建时关闭 RunAsNode、NODE_OPTIONS 和调试参数入口，并启用 ASAR 完整性校验及仅从 ASAR 加载。
- 将任务文本改为 `textContent` 纯文本渲染，避免 HTML 注入。
- 将本地存储通信限制到 `tasks`、`theme`、`alwaysOnTop`、`appearance` 四个键，并校验主题值白名单。
- 对任务结构、数量和文本长度进行校验，最多 500 条、每条 300 字。
- 任务 ID 仅接受安全字符并强制唯一，畸形或重复 ID 会被替换，避免选择器异常和批量误操作。
- 校验窗口位置和尺寸，避免损坏配置导致窗口跑到屏幕外。
- 修复托盘或窗口已销毁时可能触发的空对象错误。
- 把键盘输入事件从已弃用的 `keypress` 改为 `keydown`。
- 修复紧凑模式窗口过矮、缩放区域挤占返回按钮及连续点击导致状态错乱的问题。

## 发布前验证

- 全部 JavaScript 文件通过语法检查。
- Windows 打包成功，安装包和便携目录均已生成。
- 打包后的程序通过启动冒烟测试，自定义协议和安全熔断配置可正常工作。
- 打包后二进制的 Electron Fuses 已复核：危险的 Node 启动与调试入口关闭，ASAR 完整性及来源限制开启。
- GitHub Actions 在安装依赖后执行高危级别依赖审计，并分别生成 Windows 与 macOS 构建。
- GitHub Actions 的构建任务仅有仓库只读权限，只有发布任务具有 Release 所需的写权限。

## 保留风险

- 安装包没有商业代码签名证书，Windows 可能显示“未知发布者”。
- 待办数据保存在本机明文配置中，不适合记录密码、证件号等敏感信息。
- 应用不联网、不同步，也没有自动更新；未来继续使用时应定期更新 Electron 并重新执行依赖审计。
- GitHub Actions 使用第三方官方市场 Action 的版本标签；如需更严格的供应链防护，可进一步固定到提交哈希并启用 Dependabot。

